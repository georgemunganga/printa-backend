# Install in the nginx http context. Cloudflare/proxy real-IP handling must run
# before these zones so limits apply to the customer rather than the proxy.
limit_req_zone $binary_remote_addr zone=printa_guest_upload:10m rate=6r/m;
limit_conn_zone $binary_remote_addr zone=printa_guest_upload_conn:10m;
